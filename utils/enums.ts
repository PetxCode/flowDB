export enum HTTP {
  OK = 200,
  CREATED = 201,
  BAD = 404,
}
export enum SCHOOL {
  ADMIN = "admin",
  TEACHER = "teacher",
  STUDENT = "student",
}
export enum FOOD {
  ADMIN = "admin",
  CLIENT = "client",
  DISPATCHER = "dispatcher",
  VENDOR = "vendor",
}
